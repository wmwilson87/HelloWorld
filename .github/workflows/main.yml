# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
    paths-ignore:
      - 'workflows/**'
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: |
          echo $(date -Iseconds -u)
          echo $(date -Iseconds -u -d "1 hour ago")
          vers="Accept:application/vnd.github.v3+json"
          #commits=$(curl "https://api.github.com/repos/wmwilson87/HelloWorld/commits" -H $vers | python -c "import sys, json; print(json.load(sys.stdin))")
          commits=$(curl "https://api.github.com/repos/wmwilson87/HelloWorld/commits" -H $vers)
          commitsSHAs=$(jq -n "${commits}" | jq -r 'map(.html_url)')
          echo $(jq -n "${commitsSHAs}")
          echo $(jq -n "${commits}")

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
